name: apps/dashboard

on:
  pull_request:
    paths-ignore:
      - apps/dashboard/**

jobs:
  ci:
    runs-on: ubuntu-latest
    permissions:
      contents: none
    steps:
      - run: 'echo "Skip due to unrelated change"'
