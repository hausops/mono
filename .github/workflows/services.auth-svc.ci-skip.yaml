name: services/auth-svc

on:
  pull_request:
    paths-ignore:
      - services/auth-svc/**

jobs:
  ci:
    runs-on: ubuntu-latest
    permissions:
      contents: none
    steps:
      - run: 'echo "Skip due to unrelated change"'
