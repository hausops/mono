import type {Metadata} from 'next';

export const metadata = {
  title: 'Applications - HausOps',
} satisfies Metadata;

export default function ApplicationsPage() {
  return (
    <main>
      <h1>Applications page</h1>
    </main>
  );
}
