import type {Metadata} from 'next';

export const metadata = {
  title: 'Settings - HausOps',
} satisfies Metadata;

export default function SettingsPage() {
  return (
    <main>
      <h1>Settings page</h1>
    </main>
  );
}
