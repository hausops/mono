import type {Metadata} from 'next';

export const metadata = {
  title: 'Units - HausOps',
} satisfies Metadata;

export default function UnitsPage() {
  return (
    <main>
      <h1>Units page</h1>
    </main>
  );
}
