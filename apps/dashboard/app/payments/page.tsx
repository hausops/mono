import type {Metadata} from 'next';

export const metadata = {
  title: 'Payments - HausOps',
} satisfies Metadata;

export default function PaymentsPage() {
  return (
    <main>
      <h1>Payments page</h1>
    </main>
  );
}
